<% var global_objs = { site: site, page: page, config: config, theme: theme, _: _, path: path, url: url, env: env, body: body}; %>

<% if (!path.startsWith("index")) { %>
<%- include('normal', global_objs); %>
<% } else { %>
<%- include('terminal', global_objs); %>
<% } %>
